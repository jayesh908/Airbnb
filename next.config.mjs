/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            'avatars.gtihubusercontent.com','res.cloudinary.com'
        ]
    }
};

export default nextConfig;
